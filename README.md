# PIIP — Personal Investment Intelligence Platform

A fintech app for tracking investments, analyzing behavior, and getting AI-powered insights.

## Setup

```bash
# Server
cd server
npm install
cp .env.example .env
npm run dev

# Client (separate terminal)
cd client
npm install
npm run dev
```
